# commands/

Empty on purpose. This is where the Block 3 lab exercise has you copy in
the Day 4 slash commands (`.claude/commands/review.md` and
`.claude/commands/qa-report.md` from this repo) so they ship as part of
`taskflow-kit` instead of living only in this project's `.claude/`.

Delete this file once you've added real command files here — its only job
is to keep the directory from looking broken before you do.
