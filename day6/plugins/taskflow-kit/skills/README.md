# skills/

Empty on purpose. This is where the Block 3 lab exercise has you copy in
the Day 4 `api-endpoint` skill (`.claude/skills/api-endpoint/SKILL.md` from
this repo, skill directory and all) so it ships as part of `taskflow-kit`.

Delete this file once you've added the real skill directory here — its
only job is to keep the directory from looking broken before you do.
