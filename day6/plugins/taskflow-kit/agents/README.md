# agents/

Empty on purpose. This is where the Block 3 lab exercise has you copy in
the three reviewer agents you build in Block 2
(`security-reviewer.md`, `perf-reviewer.md`, `style-reviewer.md`) so they
ship as part of `taskflow-kit` instead of living only in this project's
`.claude/agents/`.

Delete this file once you've added the real agent files here — its only
job is to keep the directory from looking broken before you do.
